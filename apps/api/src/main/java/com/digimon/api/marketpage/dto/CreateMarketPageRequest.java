package com.digimon.api.marketpage.dto;

/**
 * POST /api/market/page 요청 본문.
 *
 * 더 이상 요청 본문을 받지 않는다(templateType 은 market_page_configs 에서 읽음).
 * git 히스토리 추적 혼란을 줄이기 위해 클래스 자체는 빈 형태로 유지한다(Q3-A).
 */
public class CreateMarketPageRequest {
}
